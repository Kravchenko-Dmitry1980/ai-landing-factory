# regression — черновые кейсы

Новые проекты перед переносом в `golden/`:

1. Создайте `<slug>/sources/` и `expected_contract.yml`.
2. Прогоните `smoke_corpus.py --project <slug> --corpus-dir ..\test_corpus\regression`.
3. После стабилизации перенесите папку в `golden/`.
